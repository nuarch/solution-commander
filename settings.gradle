rootProject.name = "mims"

include 'mims-gateway', \
        'mims-reference-data-service', \
        'mims-reference-data-ui-service'

rootProject.children.each {
  it.projectDir = file("../${it.name}")
  it.buildFileName = "build-multi.gradle"
}
